<tr class="text-gray-100">
    <td class="{{ gameround.getWinner() == 1 ? "text-green-300 font-bold" : "" }}">{{ gameround.getPlayer1Name() }}</td>
    <td>{{ gameround.getPlayer1Symbol() |rpsEmoji }}</td>
    <td class="{{ gameround.getWinner() == 2 ? "text-green-300 font-bold" : "" }}">{{ gameround.getPlayer2Name() }}</td>
    <td>{{ gameround.getPlayer2Symbol() |rpsEmoji }}️</td>
    <td>{{ gameround.getDate() | date("H:i") }}</td>
    <td>{{ gameround.getDate() | date("j.n.Y") }}</td>
    <td><form action="delete.php" method="post" class="mb-0"><input type="hidden" name="gameround_id" value="{{ gameround.getGameroundId() }}"><input type="submit" value="Löschen" class="bg-gray-700 px-2 py-1 cursor-pointer hover:bg-red-700 transition-colors rounded"></form></td>
</tr>
